browser --> SecurityFilterChain --> Controller --> Service --> DAO (Repository) --> database

Прежде чем допустить запрос пользователя до контроллера, спринг секюрити проверяет в базе данных, есть ли
 у пользователя с таким юзером право доступа